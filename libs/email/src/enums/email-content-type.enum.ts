/**
 * Email content type enumeration
 * Specifies the format of email content
 */
export enum EmailContentType {
  /** HTML formatted email content */
  HTML = 'html',
  /** Plain text email content */
  TEXT = 'text',
  /** Mixed content (both HTML and text) */
  MIXED = 'mixed',
}
